<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <data>
    <template id="runbot.build">
      <t t-call='runbot.layout'>
        <t t-set="nav_form">
          <form class="form-inline">
            <div class="btn-group">
              <t t-call="runbot.build_button">
                <t t-set="bu" t-value="build"/>
                <t t-set="klass" t-value="''"/>
                <t t-set="show_commit_button" t-value="True"/>
              </t>
            </div>
          </form>
        </t>
        <div class="row g-0">
          <div class="col-md-12">
            <t t-set="slots" t-value="build.top_parents_ids.with_context(active_test=False).slot_ids.sorted('active')"/>
            <t t-set="slots_by_batch" t-value="{slot.batch_id: slot for slot in slots}"/>
            <t t-set="batches" t-value="slots.batch_id.sorted('id')"/>
            <t t-set="bundles" t-value="batches.bundle_id"/>

            <t t-set="main_batch" t-value="from_batch or batches[:1]"/>
            <t t-set="main_bundle" t-value="main_batch.bundle_id"/>
            <t t-set="route" t-value="build._get_route_from_batch(main_batch) if main_batch else build.browse()"/>
            <t t-if="not route" t-set="route" t-value="build.ancestors"/>
            <t t-set="main_slot" t-value="main_batch.with_context(active_test=False).slot_ids.filtered(lambda slot: slot.build_id == route[:1]).sorted('active', reverse=True)[:1]"/>
            <t t-set="main_trigger" t-value="main_slot.trigger_id or route[:1].params_id.trigger_id"/>

            <!-- Breadcrumbs & Previous/Next-->
            <nav aria-label="breadcrumb" class="d-flex justify-content-between align-items-center">
              <ol class="breadcrumb">
                <li class="breadcrumb-item">
                  <a t-attf-href="/runbot/{{build.params_id.project_id.id}}" class="btn btn-link">
                    <t t-out="build.params_id.project_id.name"/>
                  </a>
                </li>
                <li t-if="main_bundle" class="breadcrumb-item dropdown">
                  <t t-if="len(batches) > 1">
                    <button type="button" class="btn btn-link dropdown-toggle" title="Other batches"
                       data-bs-toggle="dropdown" aria-expanded="false">
                      <i class="fa fa-caret-down"/>
                    </button>
                    <ul class="dropdown-menu">
                      <li t-foreach="batches.sorted(lambda b: (b.bundle_id.name, b.id))[:30]" t-as="batch">
                        <t t-set="slot" t-value="slots_by_batch.get(batch)"/>
                        <a t-attf-class="dropdown-item {{'active' if batch == main_batch else ''}}"
                           t-att-href="build.with_context(batch=batch).build_url">
                          <t t-out="batch.bundle_id.name"/> /
                          batch-<t t-out="batch.id"/>
                          (<t t-out="slot.trigger_id.name"/>)
                        </a>
                      </li>
                      <li t-if="len(batches) > 30">
                        <span class="dropdown-item text-muted">+ <t t-esc="len(batches) - 30"/> more</span>
                      </li>
                      <li>
                         <a class="dropdown-item" t-attf-href="/runbot/build/{{build.id}}/routes">
                          See all routes…
                        </a>
                      </li>
                    </ul>
                  </t>
                  <a t-att-href="main_bundle._url()" class="btn btn-link">
                    <t t-out="main_bundle.name"/>
                  </a>
                </li>
                <li t-if="main_batch" class="breadcrumb-item">
                  <a t-att-href="main_batch._url()" class="btn btn-link">
                    batch-<t t-out="main_batch.id"/>
                    <t t-if="main_trigger"> (<t t-out="main_trigger.name"/>)</t>
                  </a>
                </li>
                <li t-foreach="route.with_context(batch=main_batch)" t-as="ancestor"
                    t-attf-class="breadcrumb-item{{' active' if ancestor == build else ''}}">
                  <a t-att-href="ancestor.build_url" class="btn btn-link">
                    <t t-out="(ancestor.description or ancestor.config_id.name)[:75]"/>
                    <t t-if="len((ancestor.description or ancestor.config_id.name)) > 75">...</t>
                  </a>
                </li>
              </ol>
              <span class="btn-group pe-3">
                <a t-att-href="prev_ko.build_url" role="button" t-attf-title="Previous ko {{prev_ko.display_name}}"
                   t-attf-class="{{'' if prev_ko else 'disabled '}}btn btn-default fa fa-angle-double-left"></a>
                <a t-att-href="prev_bu.build_url" role="button" t-attf-title="Previous {{prev_bu.display_name}}"
                   t-attf-class="{{'' if prev_bu else 'disabled '}}btn btn-default fa fa-chevron-left"></a>
                <button type="button" class="btn btn-default fa fa-compress" data-toggle="hide-success" title="Toggle successful rows" aria-label="Toggle successful rows" aria-expanded="true"/>
                <a t-att-href="next_bu.build_url" role="button" t-attf-title="Next {{next_bu.display_name}}"
                   t-attf-class="{{'' if next_bu else 'disabled '}}btn btn-default fa fa-chevron-right"></a>
                <a t-att-href="next_ko.build_url" role="button" t-attf-title="Next ko {{next_ko.display_name}}"
                   t-attf-class="{{'' if next_ko else 'disabled '}}btn btn-default fa fa-angle-double-right"></a>
              </span>
            </nav>
          </div>
          <!-- Build details-->
          <t t-set="rowclass">
            <t t-call="runbot.build_class">
              <t t-set="build" t-value="build"/>
            </t>
          </t>
          <div t-attf-class="bg-{{rowclass.strip()}}-subtle {{'col-md-6' if build.children_ids or build.linked_children_build_ids else 'col-md-12'}}">
            <div class="build_details text-truncate">
              <!-- Batch/bundles links-->
              <t t-if="len(bundles) > 1">
                This build is referenced in <t t-out="len(bundles)"/> bundles<br/>
                <ul groups="base.group_user">
                  <li t-foreach="bundles" t-as="bundle" ><a t-out="bundle.name" t-attf-href="/runbot/bundle/{{bundle.id}}"/></li>
                </ul>
              </t>
              <t t-if="len(batches) > 1" groups="base.group_user">
                <b>First apparition:</b> <a t-out="batches[0].bundle_id.name" t-attf-href="/runbot/batch/{{batches[0].id}}"/><br/>
                <b>Last apparition:</b> <a t-out="batches[-1].bundle_id.name" t-attf-href="/runbot/batch/{{batches[-1].id}}"/><br/>
              </t>
              <!-- Parent -->
              <div t-if="build.parent_id and build.orphan_result">
                <i class="fa fa-chain-broken" title="Build result ignored for parent" />
                &amp;nbsp;Orphaned build, the result does not affect parent build result
              </div>

              <t t-if="build.description">
                <b>Description:</b>
                <t t-out="build.md_description"/>
                <br/>
              </t>

              <!-- Commits -->
              <t t-foreach="build.params_id.sudo()._get_batch_commit_link_ids(unique_batch)" t-as="build_commit">
                <b>Commit:</b>
                <a t-attf-href="/runbot/commit/{{build_commit.commit_id.id}}">
                  <t t-out="build_commit.commit_id.dname"/>
                </a>
                &amp;nbsp;
                <a t-att-href="'https://%s/commit/%s' % (build_commit.branch_id.remote_id.base_url, build_commit.commit_id.name)" title="View Commit on Github"><i class="fa fa-github"/></a>
                <div class="ms-3">
                  <b>Subject:</b>
                  <t t-out="build_commit.commit_id.subject"/>
                  <br/>
                  <b>Author:</b>
                  <t t-out="build_commit.commit_id.author"/>
                  <br/>
                  <t t-if="build_commit.commit_id.author != build_commit.commit_id.committer">
                    <b>Committer:</b>
                    <t t-out="build_commit.commit_id.committer"/>
                    <br/>
                  </t>
                </div>
              </t>
              <t t-if="build.params_id.config_id==build.trigger_id.light_config_id">
                <b>Mode:</b> <span class="badge btn btn-info">Light</span> <a t-if="unique_batch" t-attf-href="/runbot/bundle/{{unique_batch.bundle_id.id}}">Configure</a><br/>
              </t>
              <t t-if='more'>
                <b>Version:</b>
                <t t-out="build.params_id.version_id.name"/>
                <br/>
                <b>Config:</b>
                <t t-out="build.params_id.config_id.name"/>
                <br/>
                <b>Trigger:</b>
                <t t-out="build.params_id.trigger_id.name"/>
                <br/>
                <t t-if="build.params_id.dynamic_config.dict">
                    <b>Dynamic config:</b>
                    <details>
                        <summary><t t-out="str(build.params_id.dynamic_config.dict.get('name', '&lt;unnamed&gt;'))"></t></summary>
                        <span style="white-space: pre-wrap;"><t t-out="json.dumps(build.params_id.dynamic_config.dict, indent=4).encode('utf8').decode()"/></span>
                    </details>
                </t>
                <t t-if="build.params_id.config_data.dict">
                    <b>Config data:</b>
                    <details>
                        <summary><t t-out="', '.join(list(build.params_id.config_data.dict.keys()))"></t></summary>
                        <span style="white-space: pre-wrap;"><t t-out="json.dumps(build.params_id.config_data.dict, indent=4).encode('utf8').decode()"/></span>
                    </details>
                </t>
                <t t-if="build.params_id.modules">
                  <b>Modules:</b>
                  <t t-out="build.params_id.modules"/>
                  <br/>
                </t>
                <t t-if="build.params_id.extra_params">
                    <b>Extra params:</b>
                    <t t-out="build.params_id.extra_params"/>
                    <br/>
                </t>
                <t t-if="build.params_id.reference_build_id">
                    <b>Main build reference (parent):</b>
                    <a t-attf-href="/runbot/build/{{build.params_id.reference_build_id.id}}">
                        <t t-out="build.params_id.reference_build_id.id"/>
                    </a>
                    <br/>
                </t>
                <t t-if="build.params_id.builds_reference_ids">
                  <b>Other builds reference:</b>
                  <div t-foreach="build.params_id.builds_reference_ids" t-as="reference">
                    <a t-attf-href="/runbot/build/{{reference.id}}"><t t-esc="reference.id"/></a> - <em t-out="reference.params_id.version_id.name"/>
                  </div>
                </t>
                <t t-if="build.params_id.upgrade_to_build_id">
                  <b>Upgrade target build:</b>
                  <a t-attf-href="/runbot/build/{{build.params_id.upgrade_to_build_id.id}}">
                    <t t-out="build.params_id.upgrade_to_build_id.id"/>
                  </a>
                  <br/>
                </t>
                <t t-if="build.params_id.upgrade_from_build_id">
                  <b>Upgrade source build:</b>
                  <a t-attf-href="/runbot/build/{{build.params_id.upgrade_from_build_id.id}}">
                    <t t-out="build.params_id.upgrade_from_build_id.id"/>
                  </a>
                  <br/>
                </t>
                <t t-if="build.params_id.dump_db">
                  <b>Dump database</b>
                  <a t-attf-href="/runbot/build/{{build.params_id.dump_db.build_id.id}}">
                    <t t-out="build.params_id.dump_db.build_id.dest"/>-<t t-out="build.params_id.dump_db.db_suffix"/>
                  </a>
                  <br/>
                </t>
                <t t-if="len(build.params_id.build_ids) > 1">
                  <b>Similar builds:</b>
                  <t t-foreach="build.params_id.build_ids" t-as="simbuild">
                    <a t-if="simbuild.id != build.id" t-attf-href="/runbot/build/#{simbuild.id}">
                      <span
                        t-attf-class="badge text-bg-{{simbuild._get_color_class()}}"
                        t-out="simbuild.id"/>
                    </a>
                  </t>
                  <br/>
                </t>
                <b>Host:</b>
                <t t-out="build.host"/>
              </t>
              <div>
                <b title="Execution time of this build, without child time">
                  Build time:
                </b>
                <t t-att-tile='build.build_time' t-out="s2human(build.build_time)"/>
                <i t-if='more'>(<t t-out="build.build_time"/>s)</i>
              </div>
              <div>
                <b title='Time from creation to finish (queue time + completion time)'>
                  Wait time:
                </b>
                <t t-att-tile='build.wait_time' t-out="s2human(build.wait_time)"/>
                <i t-if='more'>(<t t-out="build.wait_time"/>s)</i>
              </div>
              <div>
                <b title='Total time '>
                  Load time:
                </b>
                <t t-att-tile='build.load_time' t-out="precise_s2human(build.load_time)"/>
                <i t-if='more'>(<t t-out="build.load_time"/>s)</i>
              </div>
            </div>
          </div>
          <div class="col-md-6" t-if="build.children_ids or build.linked_children_build_ids">
            Children:
            <table class="table table-condensed">
              <t t-foreach="(build.children_ids | build.linked_children_build_ids).sorted(lambda b: b.id)" t-as="child">
                <t t-set="rowclass">
                  <t t-call="runbot.build_class">
                    <t t-set="build" t-value="child"/>
                  </t>
                </t>
                <tr t-attf-class="bg-{{rowclass.strip()}}-subtle{{' line-through' if child.orphan_result else ''}}">
                  <td class="text-truncate" style="max-width: 0; width: 100%;">
                    <a t-attf-href="/runbot/{{'batch/%s/' % from_batch.id if from_batch else ''}}build/{{child.id}}">
                      <t t-if="child.parent_id">Build</t>
                      <t t-else="">Link</t>
                      <t t-out="child.id"/>
                    </a>
                    <t t-if="child.description">
                      <t t-out="child.md_description" />
                    </t>
                    <t t-else="">
                      with config
                      <t t-out="child.params_id.config_id.name"/>
                    </t>
                    <a groups="runbot.group_build_config_user" t-attf-href="/odoo/config/{{child.params_id.config_id.id}}">...</a>
                    <t t-if="child.orphan_result">
                      <i class="fa fa-chain-broken" title="Build result ignored for parent" />
                    </t>
                    <t t-if="child.job">
                      Running step:
                      <t t-out="child.job"/>
                    </t>
                    <t t-if="child.global_state in ['testing', 'waiting']">
                      <i class="fa fa-spinner fa-spin"/>
                      <t t-out="child.global_state"/>
                    </t>
                  </td>
                  <td>
                    <span t-attf-class="badge text-bg-info" t-out="s2human(child.build_time)"/>
                  </td>
                  <td>
                    <t t-call="runbot.build_button">
                      <t t-set="bu" t-value="child"/>
                      <t t-set="klass" t-value="'btn-group-ssm'"/>
                    </t>

                  </td>
                </tr>
              </t>
            </table>
          </div>
          <t t-set="nb_subbuild" t-value="len(build.children_ids)"/>
          <div class="col-md-12">
            <table class="table table-condensed">
              <tr>
                <th>Date (UTC)</th>
                <th>Level</th>
                <th>Message</th>
              </tr>
              <t t-set="child_by_id" t-value="{child.id: child for child in build.children_ids}"/>
              <t t-foreach="build.sudo().log_ids" t-as="l">
                <t t-set="subbuild" t-value="child_by_id.get(int(l.path)) if l.type == 'subbuild' else False"/>
                <t t-set="logclass" t-value="dict(CRITICAL='danger', ERROR='danger', WARNING='warning', OK='success', SEPARATOR='separator').get(l.level)"/>
                <tr t-att-class="'separator' if logclass == 'separator' else 'log-' + l.type" t-att-title="l.active_step_id.description or ''">
                  <td style="white-space: nowrap; width:1%;">
                    <small t-out="l.create_date.strftime('%Y-%m-%d %H:%M:%S') if l.create_date else ''"/>
                  </td>
                  <td style="white-space: nowrap; width:1%;">
                    <t t-if="l.level != 'SEPARATOR' and l.type not in ['link', 'markdown']">
                      <t t-if="l.type == 'server'">
                        <t t-set="title" t-value="build._log_details(l)"/>
                        <t t-set="href" t-value="build._get_file_url(l.path, l.line)"/>
                        <b><a t-att-href="href" t-att-title="title"><t t-out="l.level"/></a></b>
                      </t>
                      <b t-else="" t-out="l.level"/>
                    </t>
                  </td>
                  <t t-set="message_class" t-value="''"/>
                  <t t-if="subbuild" t-set="message_class">
                    <t t-call="runbot.build_class">
                      <t t-set="build" t-value="subbuild"/>
                    </t>
                  </t>
                  <td t-attf-class="bg-{{message_class.strip() or logclass}}-subtle" style="max-width: 0; width: 100%;">
                    <t t-if="l.type == 'subbuild'">
                      <a t-attf-href="/runbot/build/{{l.path}}">
                        Build #
                        <t t-out="l.path"/>
                      </a>
                      <span t-out="l.message"/>
                    </t>
                    <span t-elif="l.type == 'markdown'" t-out="l._markdown()"/>
                    <span t-elif="l.type == 'server'" t-out="build._format_message(l)" class="pre" />
                    <span t-else="" t-out="l.message"/>
                  </td>
                  <td t-attf-class="bg-{{message_class.strip() or logclass}}-subtle">
                    <t t-if="l.level in ('CRITICAL', 'ERROR', 'WARNING') and not l.with_context(active_test=False).error_content_id">
                      <small>
                        <a groups="runbot.group_runbot_admin" t-attf-href="/runbot/parse_log/{{l.id}}" class="sm" title="Parse this log line to follow this error.">
                          <i t-attf-class="fa fa-magic"/>
                        </a>
                      </small>
                    </t>
                  </td>
                </tr>
                <t t-if="l.error_content_id">
                  <t t-set="error_content" t-value="l.error_content_id"/>
                  <t t-set="error" t-value="error_content.error_id"/>
                  <tr class="log-details">
                    <td/><td/>
                    <td t-attf-class="bg-{{'info' if error.active else 'success'}}-subtle" colspan="2">
                      <small>
                        This error is already <em t-attf-title="{{'Was detected by runbot in nightly builds.' if error.active else 'Either the error is not properly fixed or the branch does not contain the fix.'}}"><t t-out="'known' if error.active else 'fixed'"/></em>.
                        <a groups="runbot.group_user" t-attf-href="/odoo/error/{{error.id}}" title="View in Backend">
                          <i t-attf-class="fa fa-list"/>
                        </a>
                        <span t-if="error.responsible and (env.user.has_group('runbot.group_runbot_error_manager') or error.responsible.id == uid)">(<i t-out="error.responsible.name"/>)</span>
                      </small>
                    </td>
                  </tr>
                </t>
              </t>
            </table>
          </div>
        </div>
      </t>
    </template>
    <template id="runbot.build_search">
      <t t-call='runbot.layout'>
        <div class="row g-0">
          <div class="col-md-12">
            <table class="table table-condensed">
              <t t-foreach="builds" t-as="build">
                <t t-set="rowclass">
                  <t t-call="runbot.build_class">
                    <t t-set="build" t-value="build"/>
                  </t>
                </t>
                <tr t-attf-class="bg-{{rowclass.strip()}}-subtle{{' line-through' if build.orphan_result else ''}}">
                  <td>
                      <t t-out="build.create_date"/>
                  </td>
                  <td>
                    <a t-attf-href="/runbot/{{'batch/%s/' % from_batch.id if from_batch else ''}}build/{{build.id}}">
                      <t t-out="build.id"/>
                    </a>
                  </td>
                  <td>
                    <t t-if="build.description">
                      <t t-out="build.md_description" />
                    </t>
                  </td>
                  <td>
                    <t t-if="build.global_state in ['testing', 'waiting']">
                      <i class="fa fa-spinner fa-spin"/>
                      <t t-out="build.global_state"/>
                    </t>
                  </td>
                  <td>
                    <span t-out="build.params_id.config_id.name"/>
                  </td>
                  <td>
                    <span t-out="build.params_id.version_id.name"/>
                  </td>
                  <td>
                    <span t-out="s2human(build.build_time)"/>
                  </td>
                  <td>
                    <t t-call="runbot.build_button">
                      <t t-set="bu" t-value="build"/>
                      <t t-set="klass" t-value="'btn-group-ssm'"/>
                    </t>
                  </td>
                  <td>
                    <t t-set="commits" t-value="build.params_id.commit_link_ids.commit_id.sorted(key=lambda c: c.repo_id.id)"/>
                    <t t-if="build_index+1 &lt; len(builds)" t-set="previous_commits" t-value="list(builds[build_index+1].params_id.commit_link_ids.commit_id.sorted(key=lambda c: c.repo_id.id))"/>
                    <t t-else="" t-set="previous_commits" t-value="[]"/>
                    <t t-foreach="zip(previous_commits, commits)" t-as="compare">
                      <t t-set="previous_commit" t-value="compare[0]"/>
                      <t t-set="commit" t-value="compare[1]"/>
                      <a t-attf-href="https://{{commit.repo_id.main_remote_id.base_url}}/compare/{{previous_commit.name}}..{{commit.name}}" t-att-title="commit.repo_id.name">
                        <i class="fa fa-plus"/>
                      </a>
                    </t>
                  </td>
                </tr>
              </t>
            </table>
          </div>
        </div>
      </t>
    </template>
    <template id="runbot.build_routes">
        <t t-call="runbot.layout">
            <div class="container-fluid">
                <h5 class="mt-2">
                    <t t-out="len(routes)"/> route(s) to build
                    <a t-attf-href="/runbot/build/{{build.id}}" t-out="build.id"/>
                    <small class="text-muted ms-1" t-out="build.description or build.params_id.config_id.name"/>
                </h5>
                <table class="table table-sm small">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Batches</th>
                            <th>Path</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr t-foreach="routes" t-as="route">
                            <td class="font-monospace text-muted" t-out="route_index"/>
                            <td>
                                <div t-foreach="route.with_context(active_test=False).slot_ids" t-as="slot">
                                    <a t-att-href="build.with_context(batch=slot.batch_id).build_url"
                                        class="font-monospace" t-out="slot.batch_id.id"/>
                                    <span class="text-muted ms-1" t-out="slot.batch_id.bundle_id.name"/>
                                    <span class="text-muted ms-1" t-out="slot.trigger_id.name"/>
                                    <span t-if="not slot.active" class="badge text-bg-secondary ms-1">archived</span>
                                </div>
                            </td>
                            <td>
                                <t t-foreach="route" t-as="segment">
                                    <t t-set="parent" t-value="route[segment_index - 1] if not segment_first else segment.browse()"/>
                                    <t t-set="link" t-value="segment.parent_link_ids.filtered(lambda l: l.parent_id == parent)[:1]"/>
                                    <t t-if="parent">
                                        <i t-if="segment.parent_id == parent" class="fa fa-angle-right text-muted mx-1" title="parent_id"/>
                                        <i t-else="" t-attf-class="fa fa-link mx-1 {{'text-warning' if link.orphan_result else 'text-muted'}}"
                                            t-attf-title="build link{{' (orphan)' if link.orphan_result else ''}}"/>
                                    </t>
                                    <a t-att-href="segment.build_url"
                                        t-attf-class="{{'fw-bolder' if segment == build else ''}} text-{{'success' if segment.global_result == 'ok' else 'danger' if segment.global_result in ('ko', 'killed') else 'muted'}}">
                                        <t t-out="segment.id"/>
                                    </a>
                                    <span t-if="segment.orphan_result" class="badge text-bg-warning ms-1">orphan</span>
                                </t>
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </t>
    </template>
  </data>
</odoo>
