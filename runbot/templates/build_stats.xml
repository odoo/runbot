<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <data>
    <template id="runbot.modules_stats">
      <t t-call='runbot.layout'>
        <div class="runbot-stats container-fluid py-4">
          <script id="triggers_data" type="application/json" t-out="json.dumps(triggers_data, indent=4, default=json_default)"/>
          <nav class="row row-cols-auto mb-4">
            <h1 class="h2" t-out="bundle.name"/>
            <div class="d-flex gap-3 ms-auto">
              <fieldset>
                <select id="trigger_id_selector" class="form-select" title="Trigger" aria-label="Trigger">
                  <option/>
                  <optgroup t-if="all_triggers" label="Triggers">
                    <option t-foreach="all_triggers" t-as="trig" t-att-value="trig.id" t-out="trig.name"/>
                  </optgroup>
                </select>
              </fieldset>
              <fieldset>
                <select id="key_category_selector" class="form-select" title="Category" aria-label="Category">
                  <option/>
                  <optgroup t-if="all_categories" label="Categories">
                    <option t-foreach="all_categories" t-as="category" t-att-value="category" t-out="category.replace('_',' ').title()"/>
                  </optgroup>
                </select>
              </fieldset>
              <fieldset>
                <select id="key_step_selector" class="form-select" title="Steps" aria-label="Steps">
                  <option/>
                  <optgroup t-if="all_steps" label="Steps">
                    <option t-foreach="all_steps" t-as="step" t-att-value="step" t-out="step.replace('_',' ').title()"/>
                  </optgroup>
                </select>
              </fieldset>
            </div>
          </nav>
          <div class="row">
            <div class="col-12 col-md-8 col-lg-9 col-xl-10">
              <div id="js-chart"/>
              <p class="text-center text-secondary my-3">Tips: click a bullet to see corresponding build stats, shift+click to center the graph on this build</p>
            </div>
            <div class="col-12 col-md-4 col-lg-3 col-xl-2">
              <div class="btn-group my-3">
                <button id="backward_button" class="btn btn-default" title="Previous Builds" aria-label="Previous Builds">
                  <i class="fa fa-fw fa-backward"/>
                </button>
                <button id="forward_button" class="btn btn-default" title="Next Builds" aria-label="Next Builds">
                  <i class="fa fa-fw fa-forward"/>
                </button>
                <button id="fast_forward_button" class="btn btn-default" title="Last Builds" aria-label="Last Builds">
                  <i class="fa fa-fw fa-fast-forward"/>
                </button>
              </div>
              <fieldset class="mb-3">
                <label for="limit_selector" class="form-label fw-bold">Number Of Builds</label>
                <select id="limit_selector" class="form-select">
                  <option value="10">10</option>
                  <option value="25">25</option>
                  <option value="50">50</option>
                  <option value="100">100</option>
                  <option value="250">250</option>
                </select>
              </fieldset>
              <fieldset class="mb-3">
                <label for="mode_selector" class="form-label fw-bold">Mode</label>
                <select id="mode_selector" class="form-select">
                  <option title="Real Values ordered by value" value="normal">Value</option>
                  <option title="Real Values ordered by name" value="alpha">Alphabetical</option>
                  <option title="Delta With Reference Build Values" value="difference">Difference</option>
                  <option title="Bigger # of datapoint varying from previous one" value="change_count">Noisy</option>
                </select>
              </fieldset>
              <fieldset class="mb-3">
                <label for="nb_dataset_selector" class="form-label fw-bold">Display</label>
                <select id="nb_dataset_selector" class="form-select">
                  <option value="-1">Custom</option>
                  <option value="0">0</option>
                  <option value="10">Top 10</option>
                  <option value="20">Top 20</option>
                  <option value="50">Top 50</option>
                </select>
              </fieldset>
              <fieldset class="mb-3">
                <label for="display_aggregate_selector" class="form-label fw-bold">Aggregates</label>
                <select id="display_aggregate_selector" class="form-select">
                  <option value="none">No</option>
                  <option value="sum">Sum</option>
                  <option value="average">Average</option>
                </select>
              </fieldset>
              <div id="js-legend"/>
            </div>
          </div>
        </div>
      </t>
    </template>
  </data>
</odoo>
